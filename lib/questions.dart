class Questions {
  late String questionText;
  late bool questionAnswer;

  Questions({required this.questionAnswer, required this.questionText}) {
    // questionText = q!;
    // questionAnswer = a!;
  }
}
